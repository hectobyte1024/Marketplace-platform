{
  "name": "marketplace-platform",
  "version": "1.0.0",
  "description": "Airbnb for Workspaces - Real-time booking and workspace marketplace",
  "private": true,
  "packageManager": "pnpm@8.0.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "test": "npm --prefix backend run test && npm --prefix frontend run test",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "type-check": "npm --prefix backend run type-check && npm --prefix frontend run type-check"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
